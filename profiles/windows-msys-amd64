# This profile is used for building on Windows, with msys. Used only for some packages
include(default)
include(common-default-options)

[settings]
arch=x86_64
arch_build=x86_64
compiler=gcc
os=Windows
os_build=Windows

[options]

[build_requires]

[env]
